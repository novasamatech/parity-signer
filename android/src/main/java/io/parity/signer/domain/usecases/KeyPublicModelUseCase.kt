package io.parity.signer.domain.usecases

import io.parity.signer.domain.KeyDetailsModel
import io.parity.signer.uniffi.ExportedSet


class KeyPublicModelUseCase {

	
//	val keyInfo = io.parity.signer.uniffi.exportKeyInfo(
//		seedName = seed,
//		exportedSet = ExportedSet.Selected(pathAndNetworks),
//	)
	fun getPublicKeyModel(): KeyDetailsModel {

	}
}
