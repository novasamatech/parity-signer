name:                     Check cargo deny

on:
  pull_request:
  push:
    branches:
      - master
      - stable
jobs:
  cargo-deny:
    name:                 Cargo deny
    runs-on:              ubuntu-latest
    strategy:
      matrix:
        checks:
          - advisories

    continue-on-error:    ${{ matrix.checks == 'advisories' }}

    steps:
      - name:               Checkout Sources
        uses:               actions/checkout@v3.5.2

      - name:               Run cargo-deny
        uses:               EmbarkStudios/cargo-deny-action@3d26fd4e2adf837aee64b12f86d4dabb799fcfa2 # v1.5.1
        with:
          command:          --manifest-path=./rust/Cargo.toml check ${{ matrix.checks }}
