[package]
name = "defaults"
version = "0.1.0"
authors = ["vera"]
edition = "2018"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
meta_reading = {path = "../meta_reading"}
definitions = {path = "../definitions"}
hex = "0.4.3"
regex = "1.4.5"
lazy_static = "1.4.0"
sp-runtime = "3.0.0"
sp-core = "3.0.0"

[lib]
name = "defaults"
crate-type = ["lib"]
